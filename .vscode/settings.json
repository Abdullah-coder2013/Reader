{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 100,
            "driver": "SQLite",
            "name": "freader",
            "database": "${workspaceFolder:Reader}/freader.db"
        }
    ]
}